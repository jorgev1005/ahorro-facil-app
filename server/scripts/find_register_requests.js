const { Client } = require('ssh2');

const config = {
    host: '75.119.154.6',
    port: 22,
    username: 'root',
    password: 'xz18219jl'
};

const REMOTE_DIR = '/var/www/ahorro_facil';

const conn = new Client();

console.log('🔍 Connecting to VPS to Search Register Requests...');

conn.on('ready', () => {
    console.log('✅ Connected.');

    const cmd = `
    echo "--- LAST 50 REGISTER REQUESTS ---"
    grep "POST /api/auth/register" /var/log/nginx/access.log | tail -n 50
    `;

    conn.exec(cmd, (err, stream) => {
        if (err) throw err;
        stream.on('close', (code, signal) => {
            conn.end();
        }).on('data', (data) => console.log(data.toString()))
            .stderr.on('data', (data) => console.log('STDERR: ' + data));
    });
}).connect(config);
