const { Client } = require('ssh2');

const config = {
    host: '75.119.154.6',
    port: 22,
    username: 'root',
    password: 'xz18219jl'
};

const conn = new Client();

console.log('🕵️‍♂️ CHECKING API LOGS FOR DEBUG PRINTS...');

conn.on('ready', () => {
    console.log('✅ Connected.');

    // Check logs for the specific debug string I added
    const cmd = `
    pm2 logs --lines 50 --nostream | grep "PUBLIC ACCESS ATTEMPT" -A 5 -B 5
    `;

    conn.exec(cmd, (err, stream) => {
        if (err) throw err;
        stream.on('close', (code, signal) => {
            conn.end();
            console.log('✅ LOG CHECK DONE');
        }).on('data', (d) => console.log(d.toString()))
            .stderr.on('data', (d) => console.log('STDERR: ' + d));
    });
}).connect(config);
